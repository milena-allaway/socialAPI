// define the models, from their respective files
const User = require('./User');
const Thought = require('./Thought');

// export the models, to be used in controllers
module.exports = { User, Thought };